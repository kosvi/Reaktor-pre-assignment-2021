// This might be poorly named file, but
// it contains the configuratons of the 
// current backend that servers data
// that has been parsed together from 
// the old bad api

// const allowedCategories = ["gloves", "facemasks", "beanies"];
const allowedCategories = ["beanies", "facemasks"];

module.exports = {
    allowedCategories: allowedCategories,
};
